---
const { title, desc } = Astro.props;
---

<header>
  <h2>{title}</h2>
  <p>{desc}</p>
</header>

<style>
  header {
    background-image: url(/images/pattern-4.avif);
    background-size: cover;
    padding: 3rem 1rem 6.25rem;

    & h2 {
      color: var(--primary-color);
    }

    & p {
      color: var(--white);
    }
  }
</style>
