---

---

<article class="service__wrap">
  <slot />
</article>

<style>
  .service__wrap {
    display: grid;
    grid-template-columns: 1fr 1fr;
    place-items: stretch;
    gap: 1rem 0.75rem;
    background-color: var(--white);
    border-radius: 2rem;
    margin-top: -3rem;
    padding: 2rem 1rem;
  }
</style>
